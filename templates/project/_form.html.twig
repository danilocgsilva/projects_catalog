{{ form_start(form) }}
    {{ form_widget(form) }}
    <button
        class="btn mt-4 px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">
        {{ button_label|default('Save') }}</button>
{{ form_end(form) }}

{# {{ form_start(form) }} #}

    {{ form_row(form.name) }}
    {{ form_row(form.databaseName) }}
    {{ form_row(form.user) }}
    {{ form_row(form.port) }}
    {{ form_row(form.host) }}
    {{ form_row(form.password) }}
    {# {{ form_widget(form) }} #}
    {# <button class="btn">{{ button_label|default('Save') }}</button> #}
{# {{ form_end(form) }} #}

{% extends 'base.html.twig' %}

{% block title %}RepositoryAddress{% endblock %}

{% block body %}
    <h1>RepositoryAddress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ repository_address.id }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ repository_address.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_repository_address_index') }}">back to list</a>

    <a href="{{ path('app_repository_address_edit', {'id': repository_address.id}) }}">edit</a>

    {{ include('repository_address/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Project{% endblock %}

{% block body %}
    <h1>Project</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_project_index') }}">Back to list</a>

    <a class="btn btn-primary" href="{{ path('app_project_edit', {'id': project.id}) }}">Edit</a>

    {{ include('project/_delete_form.html.twig') }}
{% endblock %}

{{ form_start(form, {'attr': {'id': 'submit-new-project'}}) }}
    {{ form_widget(form) }}
{{ form_end(form) }}
<input form="submit-new-project" class="btn btn-danger" type="submit" value="{{ button_label|default('Save') }}" />
